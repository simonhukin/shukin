<p>
  <b>Name:</b>
  <%= @category.name %>
</p>

<% unless @category.sub_categories.empty? %>
  <table class="sub_categories" summary="Category sub categories">
    <%= render @sub_categories %>
  </table>
<% else %>
   <p>
     This category has no sub-categories.
   </p>
<% end %>


<%= link_to 'New SubCategory', new_sub_category_path %> |
<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
